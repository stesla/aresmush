<%= header %>
<%= "%xh#{title}%xn%r%r" -%>
<% [:solid, :okay, :maybe, :missed_connection].each do |type| %>
<% unless matches[type].nil? or matches[type].empty? %>
%xh<%= type.to_s.humanize.titlecase %>:%xn <%= matches[type].map {|c| c.name}.join(', ') %>
<% end %>
<% end %>
<%= footer %>
